/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images:{
        domains:["image.tmdb.org","nextjs-movie-app.onrender.com"]
       
    }
}

module.exports = nextConfig
